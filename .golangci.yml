run:
  timeout: 5m
  skip-dirs:
    - examples
    - generator

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - gosec
    - ineffassign
    - unused
    - gocritic

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
